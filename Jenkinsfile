@Library('sample-jenkins-sl')_

pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                echo 'Hello World'
                sayHello 'Chris'
            }
        }
    }
}